version: '3.8'
services:
  dashboard:
    build: .
    container_name: agp_dashboard
    ports:
      - "5000:5000"
    volumes:
      # Monta la base de datos y otros archivos de instancia fuera del contenedor
      # para que los datos sean persistentes.
      - ./instance:/app/instance
    restart: unless-stopped
